class AppConstants {
  static const String backendBaseUrl =
      "http://10.0.2.2:5000"; // Android emulator: use host machine localhost
  static const String apiPrefix = "/api";
  static const Duration apiTimeout = Duration(seconds: 30);
}
